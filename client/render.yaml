services:
  - type: static
    name: juego-aspirantes-client
    buildCommand: cd client && npm ci && npm run build
    publishPath: client/dist
    envVars:
      - key: NODE_ENV
        value: production
